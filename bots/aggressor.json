{
  "version": 1,
  "name": "aggressor",
  "rules": [
    {
      "when": {
        "op": "and",
        "args": [
          {
            "op": "eq",
            "args": [
              { "op": "field", "path": "self.canAct" },
              { "op": "const", "value": 1 }
            ]
          },
          {
            "op": "lte",
            "args": [
              { "op": "field", "path": "opponent.distance" },
              { "op": "const", "value": 220000 }
            ]
          }
        ]
      },
      "do": { "type": "attack", "move": "strike", "band": "high" }
    }
  ],
  "default": { "type": "move", "dir": 1 }
}
